'use strict';

(function() {

    angular.module('appModule').factory('nameFactory', ['$window', function($window) {

            //Declaração de variáveis e métodos da factory
            // var nameFactory = {
            //    getX: function(value) {},  setX: function(value) {}
            //  };



            // Retornar um objeto que acesse todos os métodos
            // return nameFactory;

    }]);

})();